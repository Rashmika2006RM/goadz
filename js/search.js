// go PAGEZ Search Logic
document.addEventListener('DOMContentLoaded', () => {
    const searchForm = document.querySelector('.search-box-wrapper form');
    if (searchForm) {
        searchForm.addEventListener('submit', (e) => {
            // Default form action handles submission to all-ads.html
        });
    }
});
